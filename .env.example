# Discord bot credentials (set these as Railway environment variables in production)
DISCORD_TOKEN=your-bot-token
APPLICATION_ID=your-application-id
GUILD_ID=your-guild-id

# Optional overrides (defaults shown) — the repo the releases live in
# GITHUB_OWNER=itzshovel
# GITHUB_REPO=shovelScript
# ASSET_NAME=shovelscript.user.js
